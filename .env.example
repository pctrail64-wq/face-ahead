# FACE AHEAD — YouCam API key (optional for demo mode). NEVER commit the real key.
# Get one at https://yce.makeupar.com/api-console (hackathon = 1,000 free units)
# Journey units: aging 2u + today scan 46u (skin 16 + tone 20 + fitz 10) + future scan 16u ≈ 64u
VITE_YOUCAM_KEY=
